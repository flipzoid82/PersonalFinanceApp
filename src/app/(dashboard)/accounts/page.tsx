import { PagePlaceholder } from "@/components/page-placeholder";
export default function Page() {
  return (
    <PagePlaceholder
      title="Accounts"
      description="View connected, imported, and manually tracked accounts in one place."
    />
  );
}
