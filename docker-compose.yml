services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: personal_finance
      POSTGRES_USER: finance
      POSTGRES_PASSWORD: finance_dev_only
    ports:
      - "5432:5432"
    volumes:
      - finance_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U finance -d personal_finance"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  finance_postgres_data:
